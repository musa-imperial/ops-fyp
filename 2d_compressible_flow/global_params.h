//
extern int nx;
extern int ny;
extern int nt;
extern int ns;
extern int nf;
extern int mx;
extern int my;

// Square cylinder global variables
extern double d;
extern double radius;

extern double xlx;
extern double yly;
extern double dlx;
extern double dly;
extern double CFL, xmu, xkt, dx;

extern double um0;
extern double vm0; 
extern double tm0;

extern double xba, gma, chp, eta, uu0, dlt, um, vm, tm, x, y, dy;

// FROM INITL SUBROUTINE

extern double pi;

// param 
extern double roi, cci, tpi, chv;

// initl params
extern double ct6;

// derix, deriy, derxx, deryy params
extern double udx;
extern double udy;

extern double udxx;
extern double udyy;

// fluxx params
extern double utt;
extern double qtt;
